if getgenv().AutoKillLoaded then return end
getgenv().AutoKillLoaded = true

local library = loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/refs/heads/main/Library",
    true
))()

local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer

local AutoServerHopEnabled = true
local ServerHopInterval = 50
local LastServerHop = os.clock()

local window = library:AddWindow("Kill Hop", {
    main_color = Color3.fromRGB(150, 10, 0),
    min_size = Vector2.new(560, 700),
    can_resize = false
})

local serverTab = window:AddTab("Server Hop")

serverTab:AddSwitch("Enable Server Hop", function(state)
    AutoServerHopEnabled = state
end, true)

serverTab:AddTextBox("Hop Interval", function(value)
    local interval = tonumber(value)

    if interval and interval > 0 then
        ServerHopInterval = interval
    end
end, {
    ["clear"] = false,
    ["placeholder"] = "90"
})

local function ServerHop()
    local placeId = game.PlaceId
    local myJobId = game.JobId

    for attempt = 1, 6 do
        local success, response = pcall(function()
            return HttpService:JSONDecode(game:HttpGet(
                "https://games.roblox.com/v1/games/"
                    .. placeId
                    .. "/servers/Public?sortOrder=Desc&limit=100"
            ))
        end)

        if not success or not response or not response.data then
            task.wait(2 + attempt)
            continue
        end

        local candidates = {}

        for _, srv in response.data do
            if srv.id == myJobId then continue end
            if srv.playing >= srv.maxPlayers then continue end
            if srv.playing < 8 then continue end

            local fill = srv.playing / srv.maxPlayers

            if fill > 0.25 and fill < 0.9 then
                table.insert(candidates, srv)
            end
        end

        if #candidates == 0 then
            task.wait(3 + attempt * 1.5)
            continue
        end

        local target = candidates[math.random(1, #candidates)]

        pcall(function()
            TeleportService:TeleportToPlaceInstance(
                placeId,
                target.id,
                LocalPlayer
            )
        end)

        task.wait(5 + attempt * 2)
        return
    end
end

task.spawn(function()
    while true do
        if AutoServerHopEnabled
            and (os.clock() - LastServerHop) >= ServerHopInterval then

            LastServerHop = os.clock()
            task.spawn(ServerHop)
        end

        task.wait(1)
    end
end)


local tab = window:AddTab("Kill")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local localPlayer = Players.LocalPlayer

local tpLoop = false

local function getCharacter()
	return localPlayer.Character
end

local function getPlayersInRange()
	local character = getCharacter()
	local hrp = character and character:FindFirstChild("HumanoidRootPart")
	if not hrp then
		return {}
	end

	local inRangePlayers = {}

	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= localPlayer then
			local targetChar = plr.Character
			local targetHRP = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
			local targetHumanoid = targetChar and targetChar:FindFirstChildOfClass("Humanoid")

			if targetHRP and targetHumanoid and targetHumanoid.Health > 0 then
				local distance = (targetHRP.Position - hrp.Position).Magnitude
				if distance <= largePunchRadius then
					table.insert(inRangePlayers, targetHRP)
				end
			end
		end
	end

	return inRangePlayers
end

local function teleportToPlayers()
	task.spawn(function()
		while tpLoop do
			local character = getCharacter()
			local hrp = character and character:FindFirstChild("HumanoidRootPart")
			if not hrp then
				task.wait(0.2)
				continue
			end

			local playersInRange = getPlayersInRange()

			if #playersInRange > 0 then
				for _, targetHRP in ipairs(playersInRange) do
					if not tpLoop then
						break
					end

					hrp.CFrame = targetHRP.CFrame * CFrame.new(0, 0, 3)
					task.wait(0.2)
				end
			end
			task.wait(0.1)
		end
	end)
end

local function startTpLoop()
	task.spawn(function()
		while tpLoop do
			for _, plr in ipairs(Players:GetPlayers()) do
				if not tpLoop then
					break
				end

				if plr == localPlayer then
					continue
				end

				local targetChar = plr.Character
				local targetHRP = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
				local targetHumanoid = targetChar and targetChar:FindFirstChildOfClass("Humanoid")

				if targetHRP and targetHumanoid and targetHumanoid.Health > 0 then
					local character = getCharacter()
					local hrp = character and character:FindFirstChild("HumanoidRootPart")
					if hrp then
						-- Teleport slightly behind the target
						hrp.CFrame = targetHRP.CFrame * CFrame.new(0, 0, 3)
						task.wait(0.2)
					end
				end
			end
			task.wait(0.1)
		end
	end)
end

local switch = tab:AddSwitch("Kill Everybody", function(state)
	tpLoop = state
	if state then
		startTpLoop()
		teleportToPlayers()
	end
end)

tpLoop = true
startTpLoop()
teleportToPlayers()




local PunchBoost = {
    Current = nil 
}

local function getToolName()
    if PunchBoost.Current == "Egg" then
        return "Protein Egg"
    elseif PunchBoost.Current == "Shake" then
        return "Tropical Shake"
    end
    return nil
end

local function doPunchAndEquip()
    local player = game.Players.LocalPlayer
    local character = player.Character
    local backpack = player:FindFirstChild("Backpack")
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    local toolName = getToolName()

    if not (humanoid and backpack and toolName) then return end

    local punch = backpack:FindFirstChild("Punch") or (character and character:FindFirstChild("Punch"))
    if punch then
        pcall(function()
            humanoid:EquipTool(punch)
        end)
        pcall(function()
            player.muscleEvent:FireServer("punch", "rightHand")
        end)
    end


    local item = backpack:FindFirstChild(toolName) or (character and character:FindFirstChild(toolName))
    if item then
        pcall(function()
            humanoid:EquipTool(item)
        end)
    end
end


task.spawn(function()
    while true do
        if PunchBoost.Current then
            doPunchAndEquip()
        end
        task.wait(0.12)
    end
end)

local eggSwitch = tab:AddSwitch("Punch + Egg", function(state)
    if state then
        PunchBoost.Current = "Egg"
        if shakeSwitch then
            shakeSwitch:Set(false)
        end
    else
        if PunchBoost.Current == "Egg" then
            PunchBoost.Current = nil
        end
    end
end)


local shakeSwitch = tab:AddSwitch("Punch + Shake", function(state)
    if state then
        PunchBoost.Current = "Shake"
        if eggSwitch then
            eggSwitch:Set(false)
        end
    else
        if PunchBoost.Current == "Shake" then
            PunchBoost.Current = nil
        end
    end
end)

task.defer(function()
    task.wait(0.5) 
    if math.random(1, 2) == 1 then
        eggSwitch:Set(true)
    else
        shakeSwitch:Set(true)
    end
end)





local miscTab = window:AddTab("Misc")

local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

local configFile = "SpazOutConfig.txt"
local spazOut = false

if isfile and isfile(configFile) then
    spazOut = readfile(configFile) == "true"
end

miscTab:AddSwitch("Spaz Out", function(value)
    spazOut = value
 
    if writefile then
        writefile(configFile, tostring(value))
    end
end)


RunService.RenderStepped:Connect(function()
    if spazOut then
        Camera.CFrame = Camera.CFrame * CFrame.Angles(math.rad(15), 0, 0)
    end
end)










pcall(function()
    if switch then
        if switch.Set then
            switch:Set(true)
        elseif switch.SetState then
            switch:SetState(true)
        elseif switch.SetValue then
            switch:SetValue(true)
        elseif typeof(switch) == "table" and switch.Value ~= nil then
            switch.Value = true
        end
    end
end)

local BAR_WIDTH = 120
local BAR_HEIGHT = 20
local OFFSET = Vector3.new(0, 6.0, 0)
local BLOOD_RED = Color3.fromRGB(145, 0, 0)

local Players = game:GetService("Players")
local activeBars = {}
local barsEnabled = false
local running = false 

local function createDurabilityBar(character)
    local head = character:FindFirstChild("Head")
    local humanoid = character:FindFirstChildOfClass("Hu
